starter-django-next-docker


///////////////////////////////////////////////////////////////////стартер/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////жение
1 Создаем две папки
mkdir backend
mkdir frontend
touch .README.md
touch .dockerignore  
touch .gitignore
git init 
///////////////////////////////////////////////////////////////////BACKEND/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

2 окружение
python3.11 -m venv venv
venv/bin/activate

3 библиотека

pip install Django
pip install --upgrade pip

нужен для того, чтобы разрешить вашему фронтенду забирать данные из бэкенда.
pip install django-cors-headers

данные JSON
pip install djangorestframework

Он служит «мостом» между веб-сервером (например, Nginx) и вашим Python-кодом
pip install gunicorn

Django-приложению самостоятельно отдавать статические файлы
pip install whitenoise

постгрес
pip install psycopg2-binary


python -m pip install Pillow

вопрос ??
pip install typing_extensions

 backend pip list  

pip freeze > requirements.txt

приложение меняем settings 
startproject config .

api
python manage.py startapp api


python manage.py migrate  
python manage.py makemigrations


——————————————————http://127.0.0.1:8001/api/——————————————————DRF——————————————————————————
cd api/

touch  serializers.py


///////////////////////////////////////////////////////////////////FRONTEND/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

npx create-next-app@latest .
убираем git из фронта
rm -rf frontend/.git

npm run dev 

mkdir lib

для того чтобы ловить локально 
touch .env.local


кастомные картинки 
├── public/ 
		image

	
///////////////////////////////////JWT////////////////////////////////

создаем новое приложение authentification

в файле authentification.py создаем аунтификацию

class CookieJWTAuthentication(JWTAuthentication):

    def authenticate(self, request):
        access_token = request.COOKIES.get("access_token")

        if not access_token:
            return None

        validated_token = self.get_validated_token(access_token)

        return (
            self.get_user(validated_token),
            validated_token,
        )

подключаем в config.settings


INSTALLED_APPS = [
    'rest_framework',
    'api',
    "rest_framework_simplejwt",
    "rest_framework_simplejwt.token_blacklist",
    "authentication",#аунтификация приложение
]

##########################аунтификация #####################
REST_FRAMEWORK = {
    "DEFAULT_AUTHENTICATION_CLASSES": (
        "authentication.authentication.CookieJWTAuthentication",
    ),
}

#настройка токенов 7 дней 15 минут
SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=15),
    "REFRESH_TOKEN_LIFETIME": timedelta(days=7),

    "ROTATE_REFRESH_TOKENS": True,
    "BLACKLIST_AFTER_ROTATION": True,

    "AUTH_HEADER_TYPES": ("Bearer",),
}

в конфиге config.urls

path("api/auth/", include("authentication.urls")),# подключаем аунтификацию


authenrification.views.py
делаем вьюхи профиля смены логин
ProfileView, CookieTokenObtainPairView, CookieTokenRefreshView

далее проверяем все через терминал
python manage.py shell
----------------------ПРОВЕРКА---------------------------------------------
JWT-аутентификация построена по схеме

Login
  ↓
access_token + refresh_token
  ↓
HttpOnly Cookies
  ↓
CookieJWTAuthentication
  ↓
JWT validation
  ↓
request.user
  ↓
IsAuthenticated

Используется djangorestframework-simplejwt.

------------------------Проверка пользователя-----------------------------

python manage.py shell

from django.contrib.auth import get_user_model

User = get_user_model()

Получаем пользователя который был создан python manage.py createsuperuser 

user = User.objects.get(username="Jena")

Проверяем пароль:

user.check_password("PASSWORD")

--------ответ True--------------

-----------------------Проверка создания JWT--------------------------------------

from rest_framework_simplejwt.tokens import RefreshToken

Создаём токены
refresh = RefreshToken.for_user(user)
access = refresh.access_token

Получаем access token

str(access)

Получаем refresh token:

str(refresh)


-------------------------Проверка Access Token----------------------------------

from rest_framework_simplejwt.authentication import JWTAuthentication

Создаём authentication
auth = JWTAuthentication()


Проверяем access token:
validated_token = auth.get_validated_token(str(access))

Получаем пользователя:
auth.get_user(validated_token)

-------------------------------Проверка CookieJWTAuthentication---------------------
access_token = request.COOKIES.get("access_token")

from rest_framework.test import APIRequestFactory

factory = APIRequestFactory()
request = factory.get("/api/auth/profile/")

Передаём access token в cookie:
request.COOKIES["access_token"] = str(access)

Импортируем собственную authentication:
from authentication.authentication import CookieJWTAuthentication

Создаём authentication:
authentication = CookieJWTAuthentication()

Проверяем:
result = authentication.authenticate(request)

---------------------------------Endpoint--------------------------------------

http://127.0.0.1:8001/api/auth/profile/ Profile

http://127.0.0.1:8001/api/auth/login/  login

http://127.0.0.1:8001/api/auth/refresh/ refresh

---------------------------------Endpoint--------------------------------------
---------------------------------NEXT-API--------------------------------------
команда показать папки 
find app lib -maxdepth 4 -print


frontend/
├── app/
│   ├── login/
│   │   └── page.tsx
│   │
│   └── profile/
│       └── page.tsx
│
├── components/
│   ├── auth/
│   │   └── LoginForm.tsx
│   │
│   └── profile/
│       └── Profile.tsx
│
└── lib/
    └── api/
        └── auth.ts

auth.ts
будет отвечать за общение с Django





